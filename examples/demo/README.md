# Demo App

The demo app is a single flagship example: an account-risk queue that exercises the public `@gridra/core` and `@gridra/react` packages with a large virtualized dataset, server-side filtering and sorting, select-all-matching bulk actions, live updates, and optional grouped or tree views.

```bash
npm install --cache .npm-cache
npm run dev
```
