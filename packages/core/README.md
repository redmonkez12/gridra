# @gridra/core

Framework-agnostic primitives for query state, selection, caching, controller orchestration, and URL serialization.

## Stable Surface

Import from the package root only:

```ts
import { createGridController, createDefaultQuery } from '@gridra/core'
```

Deep imports are not supported.
