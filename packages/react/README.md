# @gridra/react

React hooks and types for building a data grid on top of `@gridra/core`.

## Stable Surface

Import from the package root only:

```ts
import { useDataGrid, useDataSource, useGridUrlSync } from '@gridra/react'
```

Deep imports are not supported.
